- if @bib and @bib.error
  %div#errors.alert.alert-danger 
    %span.glyphicon.glyphicon-exclamation-sign 
    %span#error-heading Sorry, but the LBMC Pilot system encountered a problem.
    %ul
      - @bib.error.validation_errors.each do |validation_error| 
        - if validation_error.message.include? "245"
          %li.error-message A title is required.
        - elsif validation_error.message.include? "020"
          %li.error-message There is a problem with the ISBN.  It needs to be a valid ISBN, either 10 or 13 digits long.  If the problem persists, leave the ISBN field empty.
        - else
          %li.error-message= validation_error.message
